{
  data:
  {
    dirs: [
      # "/p/DeepRacing/unpacked_datasets/local_fitting/v1/deepracing_standard",
      # # "/p/DeepRacing/unpacked_datasets/local_fitting/history_time_ablation/6second",
      "/p/DeepRacing/unpacked_datasets/local_fitting/boundary_length_ablation/200m",
    ]
  },
  comet:
  {
    # tags: ["latlong_loss", "boundary_length_ablation", "600m"]
    tags: ["ade_loss", "order5", "normloss", "200m_boundary_inputs"]
  },
  network:
  {
    kbezier : 5,
    num_segments : 10,
    with_batchnorm : True,
    heading_encoding: angle
  },
  trainer:
  {
    optimizer : Adam,
    learning_rate: 1E-4,
    momentum: 0.0,
    nesterov: False,
    betas: [0.99, 0.9999], 
    eps: 1E-8,
    weight_decay: 0.0,
    epochs: 200,
    batch_size: 512,
    float32: True,
    loss_function : ade,
    # loss_function : lat_long,
    loss_weights : [0.5, 1.5]
  }
}