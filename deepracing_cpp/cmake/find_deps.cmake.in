include(CMakeFindDependencyMacro)
find_dependency(PCL CONFIG COMPONENTS @_pcl_components_@)
find_dependency(yaml-cpp CONFIG)
find_dependency(Eigen3 CONFIG)
if(@PROJECT_NAME@_BUILT_WITH_AMENT)
    if(PCL_INCLUDE_DIRS)
        set(@PROJECT_NAME@_INCLUDE_DIRS ${PCL_INCLUDE_DIRS})
    endif()
    if(PCL_LIBRARIES)
        set(@PROJECT_NAME@_LIBRARIES ${PCL_LIBRARIES})
    endif()
endif()
if(@_waymo_tensorflow_conversion_@)
    set(protobuf_MODULE_COMPATIBLE TRUE)
    find_dependency(Protobuf)
    find_dependency(absl CONFIG)
    find_dependency(glog CONFIG)
endif()
