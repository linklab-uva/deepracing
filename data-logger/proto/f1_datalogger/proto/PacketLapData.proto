syntax = "proto3";
package deepf1.twenty_eighteen.protobuf;
import "PacketHeader.proto"
import "LapData.proto"
message PacketLapData {
		PacketHeader    m_header = 1;              // Header

		repeated LapData         m_lapData = 2;         // Lap data for all cars on track
}