syntax = "proto3";
package deepf1.twenty_eighteen.protobuf;
message MarshalZone {
		float  m_zoneStart = 1;   // Fraction (0..1) of way through the lap the marshal zone starts
		int32   m_zoneFlag = 2;    // -1 = invalid/unknown, 0 = none, 1 = green, 2 = blue, 3 = yellow, 4 = red
}